# Nibana Theme — Claude Instructions

## Pull Requests

**Always target `staging`, never `main`.** This is a standing rule with no exceptions unless explicitly overridden by the user for a specific PR.
